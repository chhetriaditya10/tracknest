import app from "./index.js"; console.log("keys",Object.keys(app)); console.log("isExpressApp", !!app.use && !!app.route); console.log("_router", app._router); if(app._router){ console.log("router length", app._router.stack.length); console.log(app._router.stack.map((layer,idx)=>( {idx, name: layer.name, path: layer.route ? layer.route.path : (layer.regexp && layer.regexp.toString()), methods: layer.route ? layer.route.methods : null} ))); }
